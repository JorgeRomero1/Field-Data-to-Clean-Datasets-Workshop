filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
p <- ggplot(plot_data, aes(x = Range, y = Row, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
scale_y_reverse() +
scale_fill_viridis_c() +
# coord_equal() +
labs(
title = paste(location_i, year_i),
x = "Range",
y = "Row",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(
legend.position = "bottom"
)
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
scale_y_reverse() +
scale_fill_viridis_c() +
# coord_equal() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(
legend.position = "bottom"
)
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year"))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
scale_y_reverse() +
scale_fill_viridis_c() +
# coord_equal() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(
legend.position = "bottom"
)
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year"))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
scale_fill_viridis_c() +
# coord_equal() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(
legend.position = "bottom"
)
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Range) - 0.5,
xmax = max(Range) + 0.5,
ymin = min(Row) - 0.5,
ymax = max(Row) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Range, y = Row, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
fill = NA,
color = "black",
linewidth = 0.8
) +
scale_y_reverse() +
scale_fill_viridis_c() +
coord_equal() +
labs(
title = paste(location_i, year_i),
x = "Range",
y = "Row",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Range) - 0.5,
xmax = max(Range) + 0.5,
ymin = min(Row) - 0.5,
ymax = max(Row) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
fill = NA,
color = "black",
linewidth = 0.8
) +
scale_fill_viridis_c() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Range) - 0.5,
xmax = max(Range) + 0.5,
ymin = min(Row) - 0.5,
ymax = max(Row) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
fill = NA,
color = "black",
linewidth = 0.8
) +
scale_fill_viridis_c() +
coord_equal() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Row) - 0.5,
xmax = max(Row) + 0.5,
ymin = min(Range) - 0.5,
ymax = max(Range) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
fill = NA,
color = "black",
linewidth = 0.8
) +
scale_fill_viridis_c() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
library(tidyverse)
library(lubridate)
library(mapview)
library(sf)
library(daymetr)
library(tigris)
library(janitor)
source("report_functions.R")
trial_data <- read_csv("../../SEASON 2023-24/01-Data/NxV/NxV_trial.csv") |>
rbind(read_csv("../../SEASON 2024-25/01-Data/NxV/NxV_trial.csv")) |>
select(-Treatment_3) |>
rename_with(~str_replace_all(.x, "%", "Pct"), contains("%"))
site_info <- readxl::read_xlsx("../metadata/Site.xlsx",
sheet = "location_info") |>
mutate(`Sowing date` = as.Date(`Sowing date`),
`Harvest date` = as.Date(`Harvest date`)
)
soil_info <- readxl::read_xlsx("../metadata/Site.xlsx",
sheet = "soil_info")
weather_daily <- load_daymet_weather(
site_info,
cache_file = "data/NxV_daymet.csv",
n_years = 25
)
rainfall_data <- prepare_cumulative_rainfall(
weather_daily,
site_info,
n_years = 25
)
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Row) - 0.5,
xmax = max(Row) + 0.5,
ymin = min(Range) - 0.5,
ymax = max(Range) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax),
inherit.aes = FALSE,
fill = NA,
color = "black",
linewidth = 0.8
) +
scale_fill_viridis_c() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
rep_boxes
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Row) - 0.5,
xmax = max(Row) + 0.5,
ymin = min(Range) - 0.5,
ymax = max(Range) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax, colour = Rep),
inherit.aes = FALSE,
fill = NA,
color = "black",
linewidth = 0.8
) +
scale_fill_viridis_c() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Row) - 0.5,
xmax = max(Row) + 0.5,
ymin = min(Range) - 0.5,
ymax = max(Range) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax, colour = Rep),
inherit.aes = FALSE,
fill = NA,
linewidth = 0.8
) +
scale_fill_viridis_c() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year")) |>
mutate(Rep = Plot %/% 100) |>
filter(!is.na(Row), !is.na(Range), !is.na(YieldDryBasis_KgHa))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
rep_boxes <- plot_data |>
group_by(Rep) |>
summarise(
xmin = min(Row) - 0.5,
xmax = max(Row) + 0.5,
ymin = min(Range) - 0.5,
ymax = max(Range) + 0.5,
.groups = "drop"
)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
geom_rect(
data = rep_boxes,
aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax, colour = as.factor(Rep)),
inherit.aes = FALSE,
fill = NA,
linewidth = 0.8
) +
scale_fill_viridis_c() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(legend.position = "bottom")
print(p)
}
yield_spatial <- trial_data |>
left_join(site_info |> distinct(Site, Year, Location), by = c("Site", "Year"))
environments <- yield_spatial |>
distinct(Site, Year, Location) |>
arrange(Year, Location)
for (i in seq_len(nrow(environments))) {
site_i <- environments$Site[i]
year_i <- environments$Year[i]
location_i <- environments$Location[i]
plot_data <- yield_spatial |>
filter(Site == site_i, Year == year_i)
p <- ggplot(plot_data, aes(x = Row, y = Range, fill = YieldDryBasis_KgHa)) +
geom_tile(color = "white", linewidth = 0.2) +
scale_fill_viridis_c() +
labs(
title = paste(location_i, year_i),
x = "Row",
y = "Range",
fill = expression("Yield (kg ha"^{-1}*")")
) +
theme_nice() +
theme(
legend.position = "bottom"
)
print(p)
}
